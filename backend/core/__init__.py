# core/ — Shared infrastructure for the Hospital Management System
# This package contains foundational components that are used across all modules:
#   core.config       — Centralized environment-based settings
#   core.database     — MongoDB + PostgreSQL connection management
#   core.auth         — Authentication decorators, helpers, and validators
#   core.utils        — Logger, serializers, and common utilities
#   core.middleware    — Error handling and request middleware
#   core.exceptions   — Custom exception classes
